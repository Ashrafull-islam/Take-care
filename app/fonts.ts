// import { Lexend, Lora } from 'next/font/google';

// export const lexend = Lexend({
//   subsets: ['latin'],
//   display: 'swap',
// });

// export const lora = Lora({
//   subsets: ['latin'],
//   display: 'swap',
// });
// utils/fonts.ts
import { Lexend, Lora } from 'next/font/google';

export const lexend = Lexend({
  subsets: ['latin'],
  display: 'swap',
});

export const lora = Lora({
  subsets: ['latin'],
  display: 'swap',
});
