// app/fonts/index.ts
import { Inter, Lora } from 'next/font/google';

export const lexend = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const lora = Lora({
  subsets: ['latin'],
  display: 'swap',
});
